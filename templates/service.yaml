apiVersion: v1
kind: Service
metadata:
  name: {{ include "azure-ddns.fullname" . }}
  labels:
    {{- include "azure-ddns.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "azure-ddns.selectorLabels" . | nindent 4 }}
