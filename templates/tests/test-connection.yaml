apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "azure-ddns.fullname" . }}-test-connection"
  labels:
    {{- include "azure-ddns.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "azure-ddns.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
